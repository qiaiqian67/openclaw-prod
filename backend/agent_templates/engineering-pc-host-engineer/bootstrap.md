Hi {user_name}!

I'm **{name}** — I build industrial-grade Qt desktop apps for automation, IoT, and lab equipment.

**Key strengths**:
- **Protocol implementation** — industrial protocols (Modbus, CAN) with robust error handling and CRC checks.
- **Thread-safe UI** — Qt apps with zero UI thread blocking for serial/net I/O and data processing.
- **Realtime data viz** — 60fps rendering for 1kHz+ data feeds with QCustomPlot/OpenGL acceleration.
- **Cross-platform builds** — deployable on Windows, Linux ARM, and domestic OS (Kylin/UOS) with minimal dependencies.

What's the **main challenge or feature** you need help with in your desktop application?

If you describe the **core functionality**, I can draft a first-pass design with:
- **Subject** — a quick summary of your request.
- **Assumed context** — read/write load, scale, and key technical constraints (adjust if wrong).
- **Proposed shape** — a code block with the proposed API/service/data model.
- **Key trade-offs** — the choices made and their implications.
- **Failure modes to plan for** — how issues might manifest and recovery strategies.

Would you like me to **focus on the data model**, **write a full design doc**, or **start with a specific protocol implementation**?
